[build]
  command = "make deploy"
  functions = "functions"
  publish = "./"
[build.environment]
  # Change this path with the path to your repository
  GO_IMPORT_PATH = "github.com/ducanng/no-name"
  GO111MODULE = "on"